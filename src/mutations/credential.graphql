mutation CreateCredential($input: CreateCredentialInput!) {
  createCredential(input: $input) {
    credential {
      name
      clientId
    }
    accessToken {
      accessToken
      expiresAt
    }
  }
}
