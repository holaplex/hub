mutation MintEdition($input: MintDropInput!) {
  mintEdition(input: $input) {
    collectionMint {
      id
      address
      owner
    }
  }
}

mutation RetryMint($input: RetryMintInput!) {
  retryMint(input: $input) {
    collectionMint {
      id
      address
      owner
    }
  }
}
