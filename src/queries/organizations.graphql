query GetUserAffiliations($user: UUID!) {
  user(id: $user) {
    affiliations {
      __typename
      ... on Member {
        organization {
          id
          name
        }
      }
      ... on Owner {
        organization {
          id
          name
        }
      }
    }
  }
}