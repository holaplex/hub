query GetOrganizationCredentials($organization: UUID!) {
  organization(id: $organization) {
    credentials {
      clientId
      name
      scopes
      createdAt
      projects {
        id
        name
      }
      createdBy {
        id
        email
        firstName
        lastName
      }
    }
  }
}
