query GetOrganizationWebhooks($organization: UUID!) {
  organization(id: $organization) {
    webhooks {
      id
      description
      events
      createdAt
      createdBy {
        id
        email
        firstName
        lastName
      }
    }
  }
}
