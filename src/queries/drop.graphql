query GetProjectDrops($project: UUID!) {
  project(id: $project) {
    id
    name
    drops {
      id
      price
      status
      createdAt
      startTime
      endTime
      collection {
        id
        supply
        totalMints
        metadataJson {
          collectionId
          name
          image
          description
          symbol
        }
      }
    }
  }
}

query GetDrop($project: UUID!, $drop: UUID!) {
  project(id: $project) {
    id
    treasury {
      wallets {
        address
        assetId
        shortAddress @client
      }
    }
    drop(id: $drop) {
      id
      price
      status
      createdAt
      startTime
      endTime
      collection {
        id
        supply
        totalMints
        blockchain
        creators {
          address
          verified
          share
          shortAddress @client
        }
        metadataJson {
          collectionId
          name
          image
          description
          symbol
        }
      }
    }
  }
}
