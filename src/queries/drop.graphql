query GetProjectDrops($project: UUID!) {
  project(id: $project) {
    id
    name
    drops {
      id
      price
      status
      createdAt
      startTime
      endTime
      collection {
        supply
        totalMints
        metadataJson {
          name
          image
          description
          symbol
        }
      }
    }
  }
}

query GetDrop($project: UUID!, $drop: UUID!) {
  project(id: $project) {
    drop(id: $drop) {
      id
      price
      status
      createdAt
      startTime
      endTime
      collection {
        supply
        totalMints
        blockchain
        creators {
          address
          verified
          share
        }
        metadataJson {
          name
          image
          description
          symbol
        }
      }
    }
  }
}
