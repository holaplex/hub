query GetCollectionMints($project: UUID!, $drop: UUID!) {
  project(id: $project) {
    drop(id: $drop) {
      id
      collection {
        id
        mints {
          owner
          ownerShortAddress @client
          address
          shortAddress @client
          creationStatus
          createdAt
        }
      }
    }
  }
}