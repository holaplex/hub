query GetProject($project: UUID!) {
  project(id: $project) {
    id
    name
    organization {
      id
      name
    }
  }
}

query GetProjectDrops($project: UUID!) {
  project(id: $project) {
    id
    name
  }
}
