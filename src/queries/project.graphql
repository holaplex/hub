query GetProject($project: UUID!) {
  project(id: $project) {
    id
    name
    organization {
      id
      name
    }
    treasury {
      wallets {
        assetId
        address
      }
    }
  }
}
