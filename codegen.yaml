overwrite: true
documents:
  - '**/*.graphql'
schema:
  - http://localhost:3005
generates:
  ./@types/graphql.d.ts:
    plugins:
      - typescript-graphql-files-modules
  ./src/graphql.types.ts:
    plugins:
      - typescript
