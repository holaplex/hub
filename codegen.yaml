overwrite: true
documents:
  - '**/*.graphql'
schema:
  - http://api.127.0.0.1.nip.io:9080/graphql
generates:
  ./@types/graphql.d.ts:
    plugins:
      - typescript-graphql-files-modules
  ./src/graphql.types.ts:
    plugins:
      - typescript
